# 端末間のデータ共有

公開URL: https://wings-minamino.github.io/project-GG/

## 初回の引き継ぎ

1. これまでデータを登録していた端末・ブラウザで公開URLを開き直す。
2. 管理者としてログインする。
3. 「このブラウザのデータを引き継ぐ」を押して人数・履歴件数を確認する。
4. 別端末でも同じURLを開いてログインする。

引き継ぎは共有データが空のときのみ可能です。既存のブラウザ保存は削除しません。元の保存キー `gacha-mission:data` と、引き継ぎ直前のコピー `gacha-mission:data:before-cloud` が端末内に残ります。複数端末の旧データは自動合算しないため、必要なデータの入ったブラウザから開始してください。

## 動作

- インターネット接続が必要です。ログイン中・表示中は5秒ごとに更新を確認し、画面へ戻った際も確認します。
- 生徒番号によるログイン、管理者の共通パスワードは従来どおりです。
- 管理者の変更は自動保存します。同時更新を検出した場合は上書きせず最新情報に戻し、再操作を案内します。
- 通信障害で結果が不明な操作は同じリクエストIDで再試行します。未保存時は画面下の「再試行」を押してください。
- 初回引き継ぎ以降、端末内の古いデータで共有データを自動的に置き換えることはありません。
- 同じタブの再読み込みでは、ログイン状態と選択中のタブを復元します。セッションはsessionStorageに保存し、復元時には必ずサーバーで有効性を確認します。パスワードは保存しません。ログアウト時には保存情報を削除します。ログインから12時間で失効し、再ログインが必要です。

## 構成

GitHub Pagesの `index.html` がSupabase Edge Function `gg-api` に接続します。プロジェクト参照は `phswoncmibtmihpmlvdg`（東京）。Edge Function内で独自セッションを検証するためプラットフォームのJWT検証は無効です。管理者操作の権限確認はサーバーで行い、生徒には他の生徒番号や景品リクエストの処理結果を返しません。生徒番号を知る人はその生徒としてログインできるという従来の仕様は維持しています。

`gg_state` はバージョン番号付きの共有状態、`gg_sessions` はハッシュ化したセッション、`gg_login_limits` はログイン頻度制限を保存します。全テーブルでRLSを有効にし、anon/authenticatedからの直接アクセスは許可していません。サーバー側のみservice_roleで読み書きします。RLSポリシーなしという情報レベルのアドバイザー通知は、この直接アクセス禁止設計によるものです。

デプロイ元は `supabase/functions/gg-api/`。サービスキーはSupabase標準の環境変数から読み、HTMLやGitHubには保存しません。データベース変更履歴はSupabaseのmigration履歴で管理しています。

## 検証

`node tests/sync.test.cjs`

認証・権限・初回移行・生徒間の分離・景品リクエストの非公開化と期限・重複防止・ガチャ回数制限・同時更新・隠しミッション報酬の重複防止・ログアウトを検証します。公開前にサーバーの管理者ログイン・読み取り・ログアウトも実環境で確認しています。

## 月間ランキング景品

日本時間で月を判定。サーバーは月替わり後の最初の認証済みアクセスで、前月の最終保存済みランキング景品と受賞者を `rankPrizeHistory` に固定し、当月の設定を空欄（未定）にする。処理はバージョン条件付きで保存し、月またぎの古い管理画面からの保存は競合として拒否する。

ランキング景品は対象月の翌月から申請できる。申請可否と景品名には確定履歴のみを使用し、翌月の設定や後日のスタンプ修正では変更しない。スタンプ数による達成景品は従来どおり達成後に申請できる。景品画面の「前月のランキング景品を確認」から前月を選べる。管理者の受け渡し一覧は申請後に対象月へ表示される。既存の申請・受け渡し記録は保持する。導入以前の未記録の景品履歴は推測しない。

ランキング景品の申請欄は受賞月の翌月に表示する（9月分は10月）。送信する申請キーは受賞月のまま維持。スタンプ景品は達成月と翌月の申請欄に表示し、日本時間の翌月10日23:59:59まで申請可。11日以降は未申請分を非表示にし、APIでも拒否する。既存の申請は期限後も保持する。
